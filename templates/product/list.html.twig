{% extends 'base.html.twig' %}
{% block title %}Tuto symfony :{% endblock %}
{% block content %}
<table class="table table-dark">
    <thead>
        <tr>
            <th  class="text-center" scope="col">id</th>
            <th  class="text-center" scope="col">Source</th>
            <th  class="text-center" scope="col">Type</th>
            <th class="text-center" scope="co">Alt</th>
            <th  class="text-center" scope="col">Prix</th>
            <th class="text-center" scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        <tr>
        {% for produit in produits %}
            <td class="text-center"><p>{{ produit.id }}</p></th>
            <td class="text-center"><a href="{{ produit.source }}" target="_BLANK">{{ produit.source }}</a></td>
            <td class="text-center"><p>{{ produit.price.type }}</p></td>
            <td class="text-center"><p>{{ produit.alt }}</p></td>
            <td class="text-center"><p>{{produit.price.labelPrix}}</p></td>
            <td class="text-center">
                <div class="row">
                    <div class="col-6">
                        <a href="{{path('products_create',{id: produit.id})}}">Update</a>
                    </div>
                    <div class="col-6">
                        <a href="{{path('products_remove',{id: produit.id})}}">Delete</a>
                    </div>
                </div>
            </td>
        {% endfor %}
        </tr>
    </tbody>
</table>
{% endblock %}